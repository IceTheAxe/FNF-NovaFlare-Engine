#if !macro
#if TOUCH_CONTROLS
// Only the stable logical ID is package-wide. Implementations declare every
// rendering, input and engine dependency beside the code that uses it.
import mobile.funkin.backend.system.input.MobileInputID;
#end
#end
